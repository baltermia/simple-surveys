services:
  postgres:
    image: postgres:17
    container_name: simple-surveys-postgres
    restart: unless-stopped
    environment:
      # Dev defaults — change for production
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: surveys
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d surveys"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
